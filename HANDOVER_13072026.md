# MARBLE ART CRM — SESSION HANDOVER
_Written: 13 July 2026 · supersedes HANDOVER_12072026.md_
_Repo root: C:\SinkS\Sinks_ART · HEAD: 07797cf on main · build GREEN (42 routes)_

## HOW WE WORK (unchanged — respect exactly)
Avshi is NOT a coder — copy-paste + architect. ENGLISH correspondence, HEBREW applications.
Complete files only (download + PowerShell move block; check project root AND Downloads).
ONE command per message. First command always `cd` to project root. Never ask Avshi to pick
a technical option — pick the safe pro one and say why in one line. Business questions
separate. NOTHING deleted without item-by-item approval. Never `npm run build` while the dev
server runs.

⚠️ **PASTE HAZARD (new, important):** Avshi's terminal paste sometimes EATS SPACES
(`git status-sb`, `Get-ChildItem-Path`, `git ls-files`). The command then errors silently and
returns nothing. **If a command returns empty output, suspect a swallowed space before
believing the result.** Send multi-part commands as SEPARATE short lines, one per code block.

---

## ✅ DONE THIS SESSION

### 1. FlowLauncher — CORRECTION to the last handover
The 12/07 handover claimed the marble wizard was "broken, flow definition missing."
**That was WRONG.** The `marble2ziv` flow WAS present in FLOWS, first in the array, and the
`useState('marble2ziv')` default matched it correctly. Nothing was missing.

The REAL bug was a **fused Tailwind class**: `className="px-3 py-1.5text-xs bg-blue-600..."`
— missing space between `py-1.5` and `text-xs`. Tailwind silently drops both, so the
"הבא ←"/"סיום ✓" button rendered with no padding and wrong text size.

**Fixed + verified working.** Also extracted all long `className` strings into `const` vars
above the JSX (Turbopack rule) so fused-class bugs can't hide in inline strings again.
Backup: `_backups/FlowLauncher_backup_13072026.tsx`.

### 2. Asaf offer math — VERIFIED, was user error, now correct
Avshi had typed the **target customer price** into the **commission** field (₪ קבוע mode).
Corrected state, confirmed to the shekel:
- עלות אמת (Ales, incl. his profit) = **₪9,699**
- עמלת Marble Art = **₪5,051** (52.1% markup on cost)
- פרמיית אמנות = 0
- **הצעה סופית ללקוח = ₪14,750** ✅
⚠️ OPEN: the `טרבלסי – הובלה` line is **0**. If transport is really being charged, this
quote is under-costed.
💡 IDEA (not built): if the ₪ קבוע commission exceeds the true cost, flag amber
"עמלה גבוהה מהעלות — בדוק". Cheap insurance against this exact typo.

### 3. 🧹 REPO CLEANUP — root went from ~110 files to **20**
Build verified GREEN after each pile. Three commits: 9c4356d → badbcaf → 07797cf.

**PILE A — quarantined (68 files, 1.2 MB) → `_backups/_pileA_13072026/`**
64 × `.b64.txt` base64 delivery envelopes (incl. `DemoCard_b64.txt`, which uses an
underscore and escaped the first sweep) · `LeadForm.tsx.removed_bak` ·
`TEST_short_call_40sec_06052026_phaseD.mp3` (488KB) · `files.zip` (was just a download
bundle — inspected, nothing unique) · `tsconfig.tsbuildinfo` (build cache, never should
have been committed).

**PILE B — moved (43 files)**
- 14 stray root `.tsx`/`.ts` → `_backups/root_strays_13072026/`
  (`DemoCard_CORRECT.tsx`, `createWorkOrderFromSketch_CORRECT.ts`, `trabelsi-po-page.tsx`,
  `OfferBuilder.tsx`, `WorkflowNav.tsx`, `MaterialCalculator.tsx`, `materialCalc.ts`,
  `materialSettings.ts`, `page.tsx`, `offer-builder-page.tsx`, `offerTypes.ts`,
  `saveOffer.ts`, `priceBreaksData.ts`, `PriceBreaksManager.tsx`)
  → **ALL CONFIRMED DEAD** — build stayed green, all 42 routes intact incl. `/` and
  `/trabelsi-po`. This closes the long-standing scar-tissue #30 risk.
- 19 old handovers → `_docs/handovers/` (git recorded as RENAMES — full history preserved)
- 5 IDEAS_PARKING variants → `_docs/ideas/` (the live 128KB `IDEAS_PARKING.md` STAYS in root)
- 2 Ziv `.xlsx` → `_docs/deals/ziv/`
- 3 phase `.sql` migrations → `supabase/migrations/`

**PILE C — deliberately LEFT in root** (they belong there / are live references):
`STATUS.md`, `NEXT_BUILD_QUEUE.md`, `CRM_ORIENTATION_AND_TASKS.md`, `SKILL.md`,
`CLAUDE.md`, `AGENTS.md`, `README.md`, `IDEAS_PARKING.md`,
`ARVO_price_offer_template.html`, `ai_video_prompts.md`, `UTM_TEMPLATES_21052026.md`.

**`.gitignore` hardened:** `_backups/` and `tsconfig.tsbuildinfo` now ignored — the two
things that caused half the mess.

**🔒 SECURITY CHECK PASSED:** `.env.local` is NOT tracked by git. Supabase keys are NOT on
GitHub.

**RESTORE:** nothing was hard-deleted. Anything can come back from
`_backups/_pileA_13072026/` or `_backups/root_strays_13072026/`. Once you've lived with the
clean repo for a week, those two folders can be emptied by hand.

---

## 🔴 TO DO NEXT (in order)

1. **Save Asaf's sink in the CRM** — wall-hung porcelain trough, **1425 × 400 × 250 mm**,
   12mm double-lamination, **150mm end walls** (basin 1125mm), hidden drain. Same build
   family as Goldman. So the job flows into pricing → and if it closes, straight into an
   Ales work order + Trabelsi PO. Estimate + renders already produced:
   `Marble art asaf estimate renders 12072026` (PDF + PNG).
2. **Asaf shop drawings** — BLOCKED on Asaf picking a colour. When he does: full ARVO set
   (plan / section / mounting / hidden-drain), same package as the Damari set.
3. **Decide the טרבלסי–הובלה = 0 line** on Asaf's quote — real or missing?
4. **Parked:** Next.js "middleware → proxy" deprecation warning on every build. Cosmetic.
   Schedule a rename session someday.

---

## 💰 ZIV DEAL STATE (unchanged — business follow-ups still OPEN)
213 → ₪12,800 SENT · 295 → ₪20,983 ready · package **₪32,433** (after ₪1,350 shared-transport
discount — **still needs Ales OK**). Trabelsi TRB-2026-0001 = both sinks · 7 sheets · ₪6,895 ·
גוון 610977.
Follow-ups: (a) Ales — confirm discount + nesting 7→6 sheets;
(b) architect — confirm counter material (terrazzo / black slate) + wall type for
Damari / Harel Sagron shop drawings.

## ✅ VERIFIED GOOD (do not re-fix)
- `/po` archive filter: `poData.ts` has `.eq('is_archived', false)` — confirmed.
- PO table clean: only **PO-2026-0019 (₪12,800)** + **PO-2026-0021 (₪20,983)** active.
- Column names: `production_orders` uses `po_number` (NOT order_number) and `agreed_cost_ils`.

## 💡 IDEAS PARKED
#14–21 in `IDEAS_PARKING.md` — shop-drawing generator, detail library, render presentations,
field-survey sheets, "any input → sketch + price" pipeline, sketch → Nano Banana, Instagram
revival, ARVO supplier-PO template, ROI פעילות cleanup.
**NEW #22:** commission-exceeds-cost amber warning in the offer builder (see §2 above).

---

## RESTART PROMPT
"Marble Art CRM, continuing. Read HANDOVER_13072026.md in repo root C:\SinkS\Sinks_ART.
Repo was cleaned 13/07 (110 → 20 root files, build green, HEAD 07797cf) — do NOT re-clean.
FlowLauncher is FIXED and working — do NOT rebuild it.
First task: save Asaf's sink spec (1425×400×250, 150mm end walls, hidden drain) into the CRM.
NOTE: my terminal paste sometimes eats spaces — send commands as separate short lines."
