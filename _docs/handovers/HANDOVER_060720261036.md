# MARBLE ART CRM — SESSION HANDOVER
_Written: 06 July 2026 · supersedes HANDOVER_05072026_EVENING.md_

## HOW WE WORK (Avshi's standing rules — respect exactly)
Avshi is NOT a coder — copy-paste + architect (application, flow, ideas). ENGLISH correspondence,
HEBREW applications. Files as DOWNLOADS + PowerShell block. **DOWNLOADS LAND IN PROJECT ROOT
(C:\SinkS\Sinks_ART), check root FIRST (memory #28).** ONE command per message. Business questions
separate. Million small pieces. NOTHING deleted without item-by-item approval. Backups /_backups/.
Long terminal pastes auto-convert to empty attachments — request PLAIN TEXT chunked (-TotalCount / -Skip).
Note: Avshi runs 3 businesses (Marble Art · Art Flooring microtopping/epoxy · industrial silicone supply) —
one unified CRM someday; for now, marble only.

## ⚠️ SCAR-TISSUE LESSONS (apply from the start)
1. "Don't see my change" → ask which URL + page; then hard-refresh Ctrl+Shift+R.
2. Sketch browser-only until שמור לגלריה + green ✓.
3. Downloads delivery only; git status before commit.
4. Type file lands before consumer. 5. Backups never in src/.
6. Never `npm run build` while dev server runs; stale .next after route moves → delete .next, rebuild.
7. Emoji anchors FAIL in console edits → ASCII anchors + base64 Hebrew + LINE-INDEX inserts.
8. Hebrew PDFs/images: wkhtmltoimage/wkhtmltopdf dir=rtl, never cairosvg.
9. .Replace() → count anchors first, abort if ≠1. **A .Replace() anchor that silently fails leaves
   HALF-EDITS — verify with a follow-up Select-String count, don't assume it worked.**
10. DB check constraints must include app enum values. 11. 'use server' exports async only.
12. Nav prefix collisions → exact-boundary isPath (=== or startsWith(href+'/')).

## ✅ DONE THIS SESSION
### Ziv PO readiness audit + cleanup (DB is now clean)
- Verified the two real Ales work orders: **PO-2026-0021 = 2.95 · ₪20,983** · **PO-2026-0019 = 2.13 · ₪12,800**.
- Caught a WRONG duplicate: PO-2026-0020 held ₪28,630 for the 2.95 (obsolete number) — ARCHIVED.
- Added `is_archived` column to production_orders (default false). Archived: 0018 (dup 213), 0020 (wrong 295),
  0015/0016/0017 (old 2.70 typo), and all empty null-label POs (0001–0014). Only 0019 + 0021 remain active.
- Trabelsi PO confirmed: **TRB-2026-0001** = both sinks · 7 sheets · ₪6,895 · גוון 610977.
- Customer offers active: MARB-2026-7599 (295 · 20,983) + MARB-2026-8543 (213 · 12,800).

### Deliverable (standalone, no code): A→Z teleprompter guide
Ziv_process_guide_A_to_Z_06072026.pdf/png — 6-phase printable walkthrough (which tab → do → see).

### Damari/Harel Sagron shop-drawing package (standalone deliverables, earlier today, no code)
Two full shop drawings (women 340 / men 320) with plan/section/elevation/steel mounting/HIDDEN DRAIN/notes;
hidden-drain = porcelain cover on 4 feet, 2-3mm perimeter gap, lift-notches both ends, inner envelope→trap.
Plus seam-alignment sheet + ARVO 2-render presentation (terrazzo/black slate). See IDEAS #17-21.

## 🔴 OPEN / UNVERIFIED — DO FIRST NEXT SESSION
1. **HALF-EDIT to fix (FlowLauncher.tsx):** the marble wizard is INCOMPLETE. The default was set
   (`useState('marble2ziv')` line 40) but the flow definition NEVER got inserted into the FLOWS array
   (the `const FLOWS: Flow[] = [` .Replace anchor failed → aborted silently). Code still works (falls
   back to FLOWS[0]) but the marble wizard doesn't appear. **FIX:** insert the marble flow block right
   after line 15 (`const FLOWS: Flow[] = [`) using line-index + base64 (block saved:
   _marble_flow_b64.txt content, base64-decode + Insert at index of the FLOWS line + 1). Then hard-refresh;
   the 🪨 "כיור: שרטוט ← אלס ← טרבלסי" button should appear first in "מה עכשיו?".
2. **VERIFY /po list filter:** earlier gave a one-line edit to poData.ts line 95 adding
   `.eq('is_archived', false)` to the list query — never confirmed it printed FIXED OK. Check that
   `/po` shows ONLY 0019 + 0021. If not, apply the filter.
3. **COMMIT once 1+2 are clean:** likely uncommitted = poData.ts (filter) + FlowLauncher.tsx (wizard).
   Nothing committed this session yet. Run git status first.

## 💡 IDEAS PARKED (in IDEAS_PARKING.md / .txt — Damari session added #17-21)
#17 shop-drawing generator (CRM) · #18 standard detail library · #19 render-presentation generator ·
#20 field-survey sheet generator · #21 "any input → working sketch + price" umbrella pipeline.
Earlier: #14 sketch→Nano Banana, #15 Instagram revival, #16 ARVO supplier-PO template, ROI פעילות cleanup.

## 💰 ZIV DEAL STATE
213 ₪12,800 SENT · 295 ₪20,983 ready · package ₪32,433 (after ₪1,350 shared-transport discount, needs
Ales OK). Both Ales POs + Trabelsi PO saved and verified. Ziv mail draft ready. Business follow-ups:
Ales confirm discount + nesting (7→6 sheets); architect confirm counter material (terrazzo/black) + wall type.

## RESTART
Fresh chat: "Marble Art CRM, continuing. Read HANDOVER_06072026.md in repo root C:\SinkS\Sinks_ART.
First: finish the marble wizard in FlowLauncher.tsx (flow block never inserted), then verify /po filter, then commit."
