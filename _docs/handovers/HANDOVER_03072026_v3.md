# MARBLE ART CRM — SESSION HANDOVER
_Written: 03 July 2026 (v3) · supersedes HANDOVER_03072026_v2.md_

Read with `CRM_ORIENTATION_AND_TASKS.md`. This = what changed + what's next.

## HOW WE WORK (Avshi's standing rule — respect exactly)
Avshi is NOT a coder — he copy-pastes and architects the application, flow, and development ideas; the bot
writes ALL code. Correspondence in ENGLISH, applications in HEBREW. Files as DOWNLOADS + PowerShell move
block — NEVER inline/base64. ONE command per message. Business/UX questions SEPARATE from commands. Many
small files ("Ferrari, million pieces"). Flag "park it" vs "build now". Backups in /_backups/ (NOT src).
NOTHING deleted without explicit item-by-item approval. Filenames filename_DDMMYYYY.ext.

---

## 🏆 SHIPPED THIS SESSION: LUMP-SUM COST MODE (the second pricing method)

**Why:** Ales quoted the Ziv 295 sink as a bulk TURNKEY price (₪18,609 incl. his profit), not day-rate.
Avshi needs BOTH methods until he and Ales settle on one ("hope in future to reach stable agreement").
Also: Avshi decided ONE price per sink (no multi-option offers like the old 295/213 PDF — keep it simple).

### The two modes (toggle per job, top of /sketch-to-offer)
- 📅 dayRate — bottom-up: days × rates + overhead + consumables + material (settings-driven, as before).
- 📦 lumpSum — Ales's ITEMIZED turnkey quote: editable labeled lines (prefilled from Avshi's real xlsx
  categories: עבודה+תקורה, סיפון, הרכבה על קיר, הובלה לת"א, טרבלסי חומר/אריזה/הובלה). Σ lines = trueCost.
Both then flow identically: ×1.15 (XYZ) → +artPremium (UVW) → 50/50 split → save actions → 3-page doc → ROI.

### VERIFIED to the shekel vs Avshi's costs_profit_Ziv_295_sink.xlsx
lump 18,609 → comm 2,791.35 → base 21,400.35 → Ziv offer 26,630 → premium 5,229.65 → split 2,614.825 each
→ ALES total 21,223.825 · AVSHI total 5,406.175. Engine reproduces every number. Test POs: 2026-0016/0017.

### Files changed (all committed + pushed)
- src/lib/pricing/alesCostTypes.ts — CostMode ('dayRate'|'lumpSum'), LumpSumLine, DEFAULT_LUMP_LINES
  (xlsx categories), PricingResult now carries costMode + lumpSumLines (snapshots get them for free).
- src/lib/pricing/alesCostCalc.ts — calcPricing branches on mode. lumpSum: trueCost = Σ lines,
  alesLaborIncome = full lump (incl. his profit). dayRate unchanged.
- src/components/pricing/PricingEngine.tsx — mode toggle; lumpSum shows itemized lines editor
  (add/remove rows); material card stays visible in lumpSum labeled "לייחוס בלבד" (compare our Trabelsi
  calc vs Ales's line). Save buttons unchanged (PricingResult carries everything).
- src/components/po/ales/AlesHeroPage.tsx — REDESIGN per Avshi: PAGE 1 = GREEN BOX ONLY (detail table
  removed — lives on page 3). Wording: "הכנסה שלך, אלס" + "כולל מע"מ" + mode-aware label (גלובלי/שכר עבודה)
  + "מתוך סך החשבונית: ₪X". Scroll-to-2-3 hint stays.
- src/components/po/ales/AlesCostPage.tsx — mode-aware: lumpSum shows the itemized Ales quote lines first
  (full transparency) + cut list as "חישוב שלנו — לייחוס"; dayRate layout unchanged. (Note: lump lines are
  rendered with keyed divs — a React key warning was fixed via anchored replace.)

### LESSON (added to scar tissue)
Do NOT run `npm run build` while the T1 dev server is running — it corrupts the shared .next folder and
pages 404 despite being in the route list. Fix: Ctrl+C the dev server, restart `npm run dev`. Either stop
T1 before building, or trust the dev server's own compile check.

---

## 🔴 NEXT (priority order)
1. **Nav links** (still open): add /sketch-to-offer + /ales-settings to WorkflowNav header menu.
2. **Engine offer → project status?** Ask Avshi: should saving a customer offer move the linked project
   to "הצעת מחיר נשלחה" in the pipeline?
3+ earlier: metal hanger module · clean הצעות nav · offer-builder Phase C · material-calc option B ·
   editable project rows.

### 💡 PARKED in IDEAS_PARKING.md
Rejected offer → drop from ROI but archive · ROI lower panels cleanup (צנרת+פעילות stale counts) ·
MEDIA→SKETCH auto-fill · Correspondence paste → dashboard מעקב היום · Whole-CRM organization pass ·
Trabelsi catalog (few hundred sheets → sheet picker into pricePerM2 seam) · Leftover/remnant inventory ·
Ales days-per-sink formula · (NEW) multi-option offers per customer — Avshi deliberately deferred; one
price per sink for now.

---

## STATE
Git clean, main up to date. Live deployed. Backups /_backups/. TODO: add /_backups/ + *_backup_* to
.gitignore. Safe to delete: HANDOVER_ales_workorder_button_RETIRED.md.

## RESTART NEXT SESSION
Fresh chat: "Marble Art CRM, continuing. Read HANDOVER_03072026_v3.md in repo root C:\SinkS\Sinks_ART.
Lump-sum mode is live and verified. Next: add nav links for /sketch-to-offer and /ales-settings."
