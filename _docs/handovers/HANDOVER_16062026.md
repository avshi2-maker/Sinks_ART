# MARBLE ART CRM — FULL HANDOVER (with ALL open bugs, nothing hidden)
**Date:** 16/06/2026 · **Project:** Sinks_ART · local `C:\SinkS\Sinks_ART` · repo `avshi2-maker/Sinks_ART` · push `main` → Vercel auto-deploy.

> ⚠️ **READ THIS FIRST — THERE IS A LIVE BUILD-BREAKING BUG RIGHT NOW.** See BUG #1. The dev server is currently failing to compile `/sketch`. Fix that before anything else.

---

## 🔴 OPEN BUGS (honest, complete)

### BUG #1 — BUILD BROKEN: duplicate import in SketchBuilder.tsx (CRITICAL, fix first)
- **Symptom:** `/sketch` fails to compile. Error: `the name 'SaveSketchToGallery' is defined multiple times` at `src/components/sketch/SketchBuilder.tsx` lines 13 and 15.
- **Cause:** While adding the gallery-save button, a PowerShell patch inserted the import line `import SaveSketchToGallery from '@/components/sketch/SaveSketchToGallery';` **multiple times** (a cleanup attempt with `.Replace()` failed on CRLF vs LF and a regex re-added copies — last count was **5 copies**).
- **FIX (do this first, do NOT use clever regex):** Open `src/components/sketch/SketchBuilder.tsx` and look at the top ~16 lines. There should be exactly **ONE** line `import SaveSketchToGallery from '@/components/sketch/SaveSketchToGallery';`. **Manually delete all duplicate copies, leaving exactly one.** Safest path given Avshi is non-coder: have the assistant read the first 16 lines (`Get-Content ... | Select-Object -First 16`), then deliver a clean full replacement of just the import block via base64, or a precise single-occurrence removal. Verify with:
  `Select-String -Path ".\src\components\sketch\SketchBuilder.tsx" -Pattern "SaveSketchToGallery" -SimpleMatch | Select-Object LineNumber`
  Expected after fix: the import appears **once** (one line number for the import) + once for the JSX usage `<SaveSketchToGallery .../>` near the buttons row. So 2 total occurrences (1 import + 1 usage). Currently there are ~6 (5 imports + 1 usage).
- **Note:** The button JSX `<SaveSketchToGallery svg={svg} spec={spec as unknown as Record<string, unknown>} defaultTitle={spec.modelName} />` WAS added correctly after "צור הזמנת ייצור" (that patch succeeded). Only the import is duplicated.

### BUG #2 — Print "about:blank" / date / page-number keeps reappearing
- **Status:** Code is CORRECT (`@page { margin: 0 }` is in `printSketch()` in SketchBuilder.tsx, line ~88, verified on disk).
- **Honest truth:** When Chrome's **"Headers and footers"** checkbox is **ON**, it overrides page CSS and forces `about:blank`/date/page# back. No code can stop that while the box is ON. When the box is **OFF**, the print is clean — confirmed working earlier this session.
- **Why it keeps coming back for Avshi:** printing opens a `window.open('', '_blank')` popup (an `about:blank` document). Chrome may treat that as a fresh context each time, so the per-site "Headers and footers = off" preference doesn't stick.
- **PROPER FIX (not yet done):** rewrite `printSketch()` to print from the same page (e.g. a hidden print-only container + `@media print` showing only the sketch, then `window.print()`), instead of a popup. Then Chrome remembers the setting and it stays clean. This is a real rework — do it fresh, test before/after. **Workaround until then: untick "Headers and footers" in the print dialog each time.**

### BUG #3 — Swapped-field supplier offer (DATA, not code)
- One `supplier_offers` row has fields crossed: `project_ref` = customer name ("זיו אלטשולר מסד עוז"), `supplier_name` = sink description ("אלס כיור 213 ס\"מ שיש 20 מ\"מ עובי"). The AI mis-routed them on capture.
- **Fix:** on `/suppliers`, ✏️ פריטים on that offer → set "עבור" field to the sink description. Optionally tighten `src/app/api/analyze-supplier/route.ts` prompt so sink spec→project_ref, fabricator name→supplier_name.

### BUG #4 — Data cleanup needed
- "אלס - קבלן ביצוע כיורים" is wrongly saved as a CUSTOMER row (should be supplier only). Plus demo rows ("פנייה משיחה", "דנה", old "Avshalom Sapir"). Archive via `/customers` 🗑️.

### NOT-YET-VERIFIED (built this session, needs a clean compile to confirm)
- The **save-sketch-to-gallery** feature (5 files written successfully — see below) has NOT been runtime-tested because BUG #1 blocks compilation. Once #1 is fixed, test: draw sketch → 🖼️ שמור לגלריה → pick customer/project or leave blank → 💾 שמור → check קטלוגים → גלריה → 📐 שרטוטים filter → card shows with edit/delete.

---

## ✅ WHAT SHIPPED & PUSHED THIS SESSION (working)

### Suppliers → customer offer pipeline (pushed, working — made 2 real offers)
- `analyze-supplier` AI route, `/suppliers` report, `PastedLeadIntake` לקוח/ספק toggle, RTL copy-texts (📋 טקסט לאלס + 📋 העתק הצעת לקוח turnkey), customer picker, **➕ שמור כלקוח חדש** button, commission (% / ₪ fixed), per-row edit/delete, customer-offer title header. Files: `src/lib/suppliers/suppliersData.ts`, `src/app/suppliers/page.tsx`, `src/components/suppliers/SuppliersReport.tsx`, `src/app/api/analyze-supplier/route.ts`.
- **Fixed mid-session:** the customers dropdown was empty because `page.tsx` never passed the `customers` prop / never called `fetchCustomersForOffer()`. Now fixed (page fetches customers + offers + suppliers, passes all three; `dynamic='force-dynamic'` + `revalidate=0`).
- Nav: **🏭 ספקים** tab added under לכידה stage in `WorkflowNav.tsx`.

### Sketch tool — double-basin + tech panel + print + blank start (pushed; but see BUG #1 which came AFTER)
- `src/lib/sketch/sketchRenderer.ts`: **double sink** (`basinCount` 1/2 — two basins, two center drains, stone divider rib = wall thickness, per-basin pitch via pitchLeftPct/pitchRightPct), **TECHNICAL DATA panel** (PAGE_H bumped 640→720, bordered two-column readout + material swatches, dark title bar drawn in SVG so it prints solid), divider labeled "מחיצה", **empty-state guard** (returns "הזן מידות להצגת השרטוט" when length/width/height not all > 0).
- `src/components/sketch/SketchBuilder.tsx`: **blank DEFAULTS** (all dims 0, modelName ''), **כיור כפול** checkbox (sets basinCount), **printSketch** rewritten with `@page margin:0`.
- These were committed/pushed BEFORE the gallery work. The gallery-button patch (after) introduced BUG #1 and is NOT yet pushed.

### Save-sketch-to-gallery (5 files WROTE to disk, NOT yet pushed, NOT yet tested — blocked by BUG #1)
Reuses the existing `demo_trials` table (no new table). **DB migration already run (Success):**
```sql
alter table public.demo_trials
  add column if not exists kind text not null default 'demo',
  add column if not exists sketch_svg text,
  add column if not exists updated_at timestamptz default now();
```
Files written (all confirmed WROTE):
- `src/lib/demos/demosData.ts` — added `saveSketchToGallery` (svg + spec→inputs_jsonb + customer_id + project_id + kind='sketch' + auto date stamp in title), `fetchCustomersForPicker`, `fetchProjectsForCustomer`; DemoTrial type gains `kind` + `sketch_svg`; `saveDemo` now sets kind='demo'.
- `src/components/sketch/SaveSketchToGallery.tsx` — NEW self-contained button+panel: customer dropdown → project dropdown (loads on customer pick) → optional title → 💾 שמור. Blank customer = general gallery asset. Props: `svg`, `spec`, `defaultTitle`.
- `src/components/demos/DemoCard.tsx` — re-delivered: renders `sketch_svg` inline (dangerouslySetInnerHTML) when kind='sketch', 📐 שרטוט badge, downloadSketch (SVG blob), keeps edit/delete.
- `src/components/demos/GalleryGrid.tsx` — NEW client filter tabs: הכל / 🎨 הדמיות / 📐 שרטוטים + grid.
- `src/app/demos/page.tsx` — re-delivered to render `<GalleryGrid demos={demos} />`.

---

## 🔜 EARLIER PLAN — ALES MOBILE RFQ TOOL (designed, DB ready, NOT built)
**This is the next big feature Avshi wants after the bugs are cleared.**

**Goal:** stop chasing Ales for scattered WhatsApp price lines. Ales opens a link on his phone, taps through a smart picker, types a price + remark, can snap a site photo, hits send → lands in `/suppliers` as a supplier_offer Avshi reviews. ~90% structured data, zero chasing.

**Decisions locked (from Avshi):**
- Ales picks from: **fixed Q&A + live CRM lists** (marble swatches, options) — "Ferrari smart, easy under the sun, editable after trial."
- Access: **open URL, no login**, BUT **+ a mini gate: one shared access code Ales memorizes** (stored as env var `ALES_ACCESS_CODE`, changeable without rebuild). Honest caveat already given to Avshi: a shared-code gate on a no-login page is a deterrent, not bank-grade; the RFQ page only ever WRITES a supplier_offer, never reads customers/quotes.
- Assets: **both directions** — Avshi attaches sketch/photos when creating the RFQ; Ales can also snap a site photo.
- Flow start: **Avshi pre-creates each RFQ** (project + assets) → Ales opens it → prices it. (Per-RFQ unique token in URL.)
- Equal basins / "Ferrari look" engineering choices are Avshi's; assistant makes safe technical calls and states them.

**CRITICAL boundary (Avshi asked, assistant confirmed):** the Ales mobile tool (intake, Ales→Avshi) and the branded PDF (output, Avshi→Ales→customer) must stay **separate**. Do NOT merge — Ales must never touch the customer-facing, commission-bearing document. Same project, two tools, by design.

**DB already created (Success) in Supabase:**
```sql
create table if not exists public.rfqs (
  id uuid primary key default gen_random_uuid(),
  token text not null unique, title_he text not null, project_ref text,
  customer_hint text, questions jsonb not null default '[]'::jsonb,
  asset_urls jsonb not null default '[]'::jsonb, status text not null default 'open',
  created_at timestamptz not null default now(), updated_at timestamptz not null default now());
create table if not exists public.rfq_responses (
  id uuid primary key default gen_random_uuid(),
  rfq_id uuid references public.rfqs(id) on delete cascade,
  answers jsonb not null default '{}'::jsonb, line_items jsonb not null default '[]'::jsonb,
  total_ils numeric not null default 0, remark_he text,
  ales_photo_urls jsonb not null default '[]'::jsonb, supplier_offer_id uuid,
  created_at timestamptz not null default now());
-- indexes + anon RLS (for all to anon using(true) with check(true)) already applied
```

**Build order (not started):** (1) DB ✓ done → (2) Avshi-side "צור RFQ לאלס" tool (create RFQ, attach assets, get shareable link) → (3) Ales mobile page `/rfq/[token]` with access-code gate + big-thumb picker + price/remark + site photo → (4) wire response → `/suppliers` as an offer to review.

---

## 📄 BRANDED PDF OFFER TEMPLATE (standalone, DELIVERED & WORKING)
- File: `ARVO_price_offer_template.html` (standalone, open in browser → paste cleaned offer text → 📄 שמור כ-PDF → email Ales who just forwards + 2 JPGs). Self-contained, ARVO gold logo embedded (base64), "א.ק עבודות גמר" in Frank Ruhl Libre, contact + עוסק מורשה 317017804 + 050-402-9723 + Aalless1290@gmail.com, auto offer# ARVO-YYYYMMDD-HHMM + timestamp, contenteditable body, print CSS hides toolbar.
- **NOT yet integrated** into the `/suppliers` הצעה ללקוח tab (planned follow-up, reuse this template). Avshi wanted this integration as a priority too.

---

## HOW TO WORK WITH AVSHI (critical — he got frustrated this session)
- **NON-CODER.** Copy-paste only. **NEVER ask him to interpret output** ("did it say X or Y?") or make code decisions. Ask only plain BUSINESS/UX questions.
- Tell him: "paste this, tell me the LAST line it printed." One command at a time.
- **Two terminals:** T1 = `npm run dev` (cd to project first). T2 = runs given commands.
- **File delivery — USE BASE64, it's mangle-proof.** Present a `.b64.txt`, he downloads, decode with:
  `$bytes=[System.Convert]::FromBase64String((Get-Content -LiteralPath $f -Raw).Trim()); [System.IO.File]::WriteAllBytes($dest,$bytes)`
  Hebrew/apostrophes in PowerShell single-quoted here-strings = corruption. Don't.
- **PowerShell `.Replace()` and regex on CRLF files are unreliable** — they silently add/miss. **BUG #1 was caused by exactly this.** Prefer full-file base64 replacement over in-place string surgery for anything non-trivial. Always verify with `Select-String ... | Select LineNumber` and show the COUNT.
- **DON'T GUESS — READ THE FILE** before proposing a cause. Multiple hours were lost this session guessing instead of reading.
- Real compile verdict = T1 dev server, not isolated `tsc`.
- DOCX RTL is abandoned (opens LTR). Use HTML→browser Save-as-PDF, and RTL copy-text→Notepad→Outlook.

## STACK / IDS
- Next.js 16.2.x + Turbopack, TS, Tailwind v4, Hebrew RTL. Supabase `givcxgzhfoetujhrjgvc` (shared w/ public site, anon RLS). Claude API model `claude-sonnet-4-6`. Cloudinary `uploadToCloudinary(file, folder)`, folder 'Demo-Trials'.
- Ales = fabricator/SUPPLIER (ARVO / א.ק עבודות גמר, עוסק מורשה 317017804, 0504029723, Aalless1290@gmail.com). Avshi = marketing/sales PROMOTER only, invoices Ales commission after completion. Customer prices FINAL/VAT-incl (18%), never show VAT breakdown.
- Turbopack JSX: opening tags all-attributes-+`>` on ONE line.

## STANDING RULES
Never delete tables/code without approval. Filenames include date DDMMYYYY. Pick best pro+safe technical option, state rationale 1-2 sentences, proceed; only ask BUSINESS/UX questions. WhatsApp URLs `api.whatsapp.com/send` never wa.me. Every commit feature-complete. Never defer required components.

## IMMEDIATE NEXT STEPS (in order)
1. **Fix BUG #1** (duplicate import) → get `/sketch` compiling.
2. Test the save-sketch-to-gallery feature (draw → 🖼️ שמור לגלריה → check גלריה 📐 שרטוטים).
3. Commit + push the gallery feature once verified.
4. Then Avshi's choice: branded-PDF→/suppliers integration, OR the Ales mobile RFQ tool (steps 2-4), OR the proper print-popup fix (BUG #2).
