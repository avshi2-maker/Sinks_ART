# MARBLE ART CRM — SESSION HANDOVER
_Written: 03 July 2026 (v2) · supersedes HANDOVER_03072026.md_

Read with `CRM_ORIENTATION_AND_TASKS.md`. This = what changed + what's next.

## HOW WE WORK (Avshi's standing rule — respect exactly)
Avshi is NOT a coder — he copy-pastes and architects the application, flow, and development ideas; the bot
writes ALL code. Correspondence in ENGLISH, applications in HEBREW. Deliver files as DOWNLOADS + a PowerShell
move block — NEVER inline/base64 in chat. ONE command per message. Ask business/UX questions SEPARATELY from
commands. Build in MANY small files ("Ferrari, million pieces"). Flag "park it" vs "build it now" to prevent
scope drift. Backups in /_backups/ (NOT src). NOTHING deleted (data or files) without explicit approval,
shown item-by-item first. Filename convention filename_DDMMYYYY.ext.

---

## 🏆 SHIPPED THIS SESSION: customer-offer button — the pricing pipeline is now COMPLETE end-to-end

Sketch → pricing engine → (a) Ales 3-page work order  AND  (b) customer offer into the existing quotes flow.

### The customer-offer wiring (reuses existing system — NO duplicate)
- `src/lib/offers/createOfferFromEngine.ts` (NEW) — bridge: engine PricingResult → existing `saveOffer()`.
  Maps: grand = finalOfferIls (UVW, customer price) · commission→ROI total_margin = avshiTotalIls (Avshi's
  FULL take = 15% + his premium share) · cost = grand − commission. Customer summary = CLEAN price only
  (no cost breakdown, no split — customer never sees page 3). Lands in quotes / offers-sent / ROI.
- `src/components/pricing/PricingEngine.tsx` — the greyed "בקרוב" button is now LIVE:
  📄 צור הצעת מחיר ללקוח → saves via bridge → confirms "✓ נשמרה · MARB-YYYY-XXXX" + link to /quotes/[id].
  🔧 work-order button unchanged.
- Existing `saveOffer.ts` / `quotes` table / `offerTypes.ts` reused as-is (writes total_grand/total_cost/
  total_margin; total_margin is what ROI reads).

### VERIFIED end-to-end (Goldman)
Engine → 📄 → quote MARB-2026-2617 saved: total_grand 15,813 · total_margin 3,910 · customer מסד עוז בע"מ.
ROI board now shows it clean: הכנסה 15,813 · רווח 3,910 (25%) · עלות 11,903. Numbers match the 3-page doc.
NOTE: 15,813 is CORRECT (final price incl the 5,000 art premium) — not a bug; it just looks high because of premium.

### DATA CLEANUP done this session (with explicit per-row approval)
Deleted 4 old test/demo quotes (אבשי test, both ורד, דודו בלחסן) via SQL (quote_lines first, then quotes,
explicit ids). Left ONLY today's Goldman offer so ROI reads clean. This was a one-time manual cleanup.

---

## 🔴 NEXT (priority order)

### 1. Add nav links for the new pages (quick, still open from last session)
/sketch-to-offer + /ales-settings exist but are NOT in the header menu (reached via 🧮 button + direct URL).
Add to WorkflowNav (הצעות group). Check WorkflowNav.tsx.

### 2. Consider: engine offer should it also set project status / feed pipeline?
When a customer offer is saved, should the linked project move to "הצעת מחיר נשלחה"? Ask Avshi. (Currently
just creates the quote.)

### 3+ earlier open: metal hanger module · clean הצעות nav (retire OLD הצעת מחיר מהירה, move old /quotes) ·
offer-builder Phase C · material-calc option B · editable project rows.

### 💡 PARKED in IDEAS_PARKING.md (build later, NOT now) — grew this session
- **REJECTED OFFER → drop from ROI but ARCHIVE** (NEW): click "נכשל" → quote status='rejected' → ROI query
  (roiData.ts) filters it out (WHERE status NOT IN rejected/lost) → offer kept + badged, reversible. This is
  the PROPER long-term way to keep ROI clean without deleting (replaces today's manual delete approach).
- **ROI lower panels cleanup** (NEW): צנרת פרויקטים + פעילות carry stale test/demo project+customer counts.
  Audit like we did quotes; add demo/archived filter so panels count only REAL work.
- MEDIA→SKETCH auto-fill (JPG+MP4 lead model → sketch dims, review before save).
- CORRESPONDENCE paste → dashboard מעקב היום (subject+body+follow-up date).
- WHOLE-CRM ORGANIZATION PASS (audit every tab, live session, nothing deleted without approval).
- TRABELSI CATALOG (few-hundred sheets) → plugs into materialCalc pricePerM2 seam via sheet picker.
- LEFTOVER/REMNANT INVENTORY · Ales days-per-sink formula.

---

## STATE
Git clean, main up to date with origin. Live deployed. Backups /_backups/. Untracked safe-to-delete:
HANDOVER_ales_workorder_button_RETIRED.md, and a leftover temp ideas_roi_lower_panels.txt at root (its
content is already appended to IDEAS_PARKING.md — the temp file can be deleted).
TODO cleanup: add /_backups/ + *_backup_* to .gitignore.

## RESTART NEXT SESSION
Fresh chat: "Marble Art CRM, continuing. Read HANDOVER_03072026_v2.md in repo root C:\SinkS\Sinks_ART.
Pricing pipeline is complete (sketch→engine→Ales doc + customer offer). Next: add nav links for
/sketch-to-offer and /ales-settings."
